# SDD ledger — plan: docs/superpowers/plans/2026-09-03-rcedit-v2.md

Setup: 2026-09-03. Spec read: docs/superpowers/specs/2026-09-03-rcedit-v2-design.md (authority). Plan: 11 tasks.
Ruling: work directly on master (no worktree/branch) — user explicitly chose "Work on master" when asked — cost if wrong: rewrite commits land on master and must be reverted/rebased rather than dropped with a branch.

## Environment (corrected from TODO.md)
- VS dev shell: `& "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64`  (TODO's `2022\<Edition>` path is WRONG on this machine — only VS 18 BuildTools is installed).
- Toolchain: MSVC 14.51.36231 (C++23 capable), bundled CMake 4.3.1-msvc1, bundled Ninja — all reach the shell after the dev-shell launch. `cl` needs the dev shell (INCLUDE/LIB), not just PATH.
- No submodules present yet (`.gitmodules` empty). Task 1 adds `external/vcpkg`.
- Old rcedit v1 tree present under src/ (src/cmd, src/utils, src/main.cpp) + top-level CMakeLists.txt — Task 1 deletes it.
- Commit identity: `git -c user.name=fabienfl -c user.email=fabien.fl-orc@ssi.gouv.fr commit ...` (do not write to config).

## Pre-flight conflict scan
Full tables: preflight-scan.md. Verdict: interfaces match end-to-end; 20 intra-task/plan-vs-spec defects. Rulings below (spec = binding authority; TODO decisions cited where plan contradicts itself). Note: scan agent spawned its own research subagents (one overwrote the report file); result hand-verified and sound. Implementers carry the no-subagents contract.

### Rulings (rule on each before execution)
- Ruling R1 (#1 exe output path) — Task 1 sets `set(CMAKE_RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/$<CONFIG>")` at top level so rcedit.exe / rcedit_tests.exe land at `build/<preset>/<Config>/`, making T1 Step-10 and all T11 CI/PDB paths correct as written. Cost if wrong: exe path mismatch, caught at T1/T11 build.
- Ruling R2 (#3 RC language) — Task 1 uses `project(rcedit VERSION 2.0.0 LANGUAGES C CXX RC)` (enable RC) so T7's fixture.rc compiles under Ninja Multi-Config. Cost if wrong: T7 configure fails.
- Ruling R3 (#17 T11 PDB / Files-header gap) — Task 1 front-loads PDB generation for release: `/Zi` compile + linker `/DEBUG /OPT:REF /OPT:ICF`, so rcedit.pdb exists for MinSizeRel and T11 need not edit CMakeLists. Cost if wrong: no PDB for artifact, caught at T11.
- Ruling R4 (#2) — Task 6 uses `HRESULT_FROM_WIN32(ERROR_NEGATIVE_SEEK)` (the plan's `HRESULT_WIN32_ERROR_NEGATIVE_SEEK` is undefined). Cost: compile fail at T6.
- Ruling R5 (#5 comsuppw / link policy) — The IMPORT ALLOWLIST (KERNEL32 always; OLEAUT32 only w/ 7z) is the binding gate, enforced by the `imports` CTest. Static libs that add NO new DLL import (uuid.lib; comsuppw.lib if the linker demands `_com_issue_error`) are PERMITTED with 7z — consistent with TODO decision (line 33). "Nothing else, ever" = no new DLL-importing lib / no new DLL in the import table. If a link pulls a symbol requiring a NEW DLL, STOP and report symbol names. Implementer must report if it adds comsuppw and confirm the imports CTest still passes. Binds T1 (link list = kernel32+CRT, +oleaut32+uuid w/ 7z), T6, T10. Cost: at worst a spurious stop or an unexpected import — the imports CTest catches any real DLL import regardless.
- Ruling R6 (#6 zstd target) — Task 5 prefers `zstd::libzstd_static` (spec + static-only); fall back to `zstd::libzstd` only if the former isn't exported; verify against installed zstd config and report which was used. Cost: link fail at T5.
- Ruling R7 (#7/#10 T8 hexdump) — Task 8 derives the GOLDEN hexdump output for the 7-byte "fixture" from the spec's format (standard `hexdump -C`/xxd style), then makes BOTH impl and test match it. Do not trust the plan's 31-space test literal nor the 30-space impl blindly. Cost: red test, caught at T8.
- Ruling R8 (#8 T6 Seek dup) — Task 6 factors the shared seek math into one helper used by both streams. Quality, low cost.
- Ruling R9 (#9 7z entry name) — Keep fixed internal entry name `L"payload"`; decompression reads entry index 0, so the name never surfaces. Codec::Compress has no name param; document the deviation in a comment. Cost: none (cosmetic archive metadata).
- Ruling R10 (#10 case-fold dup) — Task 2 adds `EqualsIgnoreCase(wstring_view,wstring_view)` to encoding.h/.cpp (consistent case-folding via towlower); Task 3 and Task 4 consume it instead of each reimplementing. Cost: small added surface in T2.
- Ruling R11 (#11 T9 label dup + magic 18) — Task 9 extracts one option-label formatter and computes column width from content, not a hardcoded 18. Cost: minor.
- Ruling R12 (#12 T10 ParseLimit overflow + unchecked expected) — Task 10 guards ParseLimit against size_t overflow (usage error on overflow, mirroring ParseLang's bound check) and has Run* handlers check `.has_value()` defensively rather than rely solely on validate. Cost: minor robustness.
- Ruling R13 (#13/#4/#16 undercounted deviations) — ACCEPTED intended deviations (internally consistent, no code change): `ParseResourceName/FormatResourceName` naming; `OptionSpec.valueName` 6th field; `ParsedArgs.values` key type; `errc::empty_payload`. Added to the intended-deviation list handed to reviewers so they aren't flagged as spec violations. Cost: none (doc).
- Ruling R14 (#14 zstd checksum) — Advanced CCtx API + `ZSTD_c_checksumFlag=1` allowed (frames are self-produced; adds integrity); require an explanatory code comment. Cost: negligible.
- Ruling R15 (#17 four formatters) — YAGNI: deliver only `FormatError` + `formatter<ResourceId>` (the used ones); do NOT add unused formatter<ResourceKey/error_code/path>. Accepted deviation. Cost: none; add later if needed.
- Ruling R16 (#15 CI artifact) — Task 11 gates artifact upload on `startsWith(github.ref,'refs/tags/')` AND preset==default (spec: "on tags"); avoid unsanitized ref in artifact name. Cost: none.
- Ruling R17 (#7 top / T11 README) — Task 11 README states presets accurately: only `default` has `-Debug` and `-RelWithDebInfo`; no-7z/no-zstd/minimal have `-MinSizeRel` only — describe from what T1 actually produced. Cost: doc accuracy.
- Ruling R18 (#18 T4 under-asserted test) — Task 4's `FindCodecMatchesBuildFlags` None case asserts the specific documented error code, not just `!has_value()`. Cost: minor test rigor.

## Execution log
BASE (branch start) = f5afff952332843bf160e4344edb45e32f934857
Task 1: dispatched (implementer agent a9c688ddc905e4036, sonnet) with rulings R1/R2/R3 folded in + R5 link note + corrected dev-shell path. BASE for review = f5afff9.
Task 1: implementer DONE, commit ec147cc. version+imports CTests pass on minimal-MinSizeRel and default-MinSizeRel; imports = KERNEL32 only on both (nothing calls 7z yet — correct). Self-noted: added explicit CMAKE_CONFIGURATION_TYPES incl. MinSizeRel (Ninja MC omits it by default); wrapped import check in rcedit_add_import_check(); GIT_CONFIG_GLOBAL scratch override for network-share submodule ownership (no persistent config). Reviewer dispatched (agent a98ea543217a5a6b0, sonnet).
Note: repo clangd has no C++23/include config → emits false positives (std::print/span/string_view "not found", nested-namespace "C++17"). Authoritative = MSVC build passing. Ignore clangd standard/include diagnostics for all tasks.
Task 1: review clean — Spec ✅, quality Approved. Task 1: minor (deferred): CMakeLists CMAKE_CONFIGURATION_TYPES uses CACHE ... FORCE, overwrites user -D override; prefer `if(NOT CMAKE_CONFIGURATION_TYPES)`.
Task 1: complete (commits f5afff9..ec147cc, review clean).
Task 2: dispatched (implementer agent ad10e554e6f90b35b, sonnet) with delta R10 (EqualsIgnoreCase in encoding). BASE for review = ec147cc. Awaiting report.

Task 2: implementer DONE, commit b67129f. 4/4 CTest groups (version, error, encoding, imports) pass on minimal+default MinSizeRel, pristine. R10 EqualsIgnoreCase in place; folds ASCII only (towlower/"C" locale) — fine for ASCII consumers (aliases, codec names); noted for T3/T4. Reviewer dispatched (agent a10974d03e1ecf271, sonnet). BASE for review = ec147cc.
Task 2: review clean — Spec ✅, Approved. Minors (deferred, both match brief, not defects): guard.h has no automated test yet (guards verified later); FileHandle is hand-rolled RAII lacking release()/reset() (may matter for T7 engine).
Task 2: complete (commits ec147cc..b67129f, review clean).
Task 3: dispatched (implementer agent acd0360d182c2f605, sonnet). Delta: consume R10 EqualsIgnoreCase; accepted deviations R13/R15. BASE for review = b67129f. Awaiting report.
Task 3: implementer DONE, commit 89ccdcf. 5/5 CTest groups pass on minimal+default; resource_id 14/14; pristine. Consumed EqualsIgnoreCase, no dup case-fold. Reviewer dispatched (agent a54e8e1afec229d8d, sonnet). BASE for review = b67129f.
Task 3: review clean — Spec ✅, Approved, no issues.
Task 3: complete (commits b67129f..89ccdcf, review clean).
Task 4: dispatched (implementer agent aad1bb90cd26dd596, sonnet). Deltas R10 (EqualsIgnoreCase in ParseCodecName), R18 (None asserts specific errc). BASE for review = 89ccdcf. Awaiting report.
NOTE (build gating): codecs come online incrementally, so the linkable preset per task is:
  after T4: minimal only (FindCodec refs undefined ZstdCodec/SevenZipCodec under enabled presets — EXPECTED).
  after T5 (zstd): minimal + no-7z.  after T6 (7z): all four (default, no-7z, no-zstd, minimal).
  Gate each codec task on its linkable preset; full default/no-zstd validation happens at T6.
Task 4: implementer DONE, commit 3e7cfed. minimal 6/6 CTest groups pass (codec 4 cases) pristine; default compiles guarded branches, tests exe fails to LINK on undefined ZstdCodec/SevenZipCodec — EXPECTED (T5/T6 add them). Reviewer dispatched (agent a81397c1e4e8f3f4a, sonnet). BASE for review = 89ccdcf.
Task 4: review clean — Spec ✅, Approved, no issues. R18 None case asserts std::errc::invalid_argument (documented contract). Guards use RCEDIT_HAS_ZSTD/RCEDIT_HAS_7Z (derived from CMake options).
Task 4: complete (commits 89ccdcf..3e7cfed, review clean).
Task 5: dispatched (implementer agent af83697f4f8eebab7, sonnet). Deltas R6 (static zstd target), R14 (checksum comment). Gate no-7z. BASE for review = 3e7cfed. Awaiting report.
Task 5: implementer DONE, commit 7d5531a. no-7z 6/6 (ZstdRoundTrip/RejectsEmpty/RejectsCorrupt), minimal 6/6 (zstd out), imports=KERNEL32 only, pristine. R6: linked zstd::libzstd_static (both exported; chose static per ruling, commented). R14: checksum comment added. Reviewer dispatched (agent a8ae6afaf383df572, sonnet). BASE for review = 3e7cfed.
Task 5: review — Spec compliant on rulings, but ONE Important finding: codec_zstd.cpp streaming-decompress (unknown-content-size branch) ignores last ZSTD_decompressStream return (0=complete vs >0=incomplete), so a truncated frame lacking embedded content size returns success instead of errc::corrupt_payload. Latent (self-produced frames embed content size → other branch). Enters fix loop. FIX_BASE = 7d5531a.
Task 5: fix round 1 DONE, commit e861749 (track last ZSTD_decompressStream rc; new tests ZstdStreamingFrameRoundTrip/RejectsTruncation, verified red first). no-7z codec 9/9, minimal 6/6. Scoped re-review dispatched (agent aaa0a47c733f8259e, sonnet). FIX_BASE=7d5531a HEAD=e861749.
Task 5: fix round 1 re-review — ADDRESSED (last-rc tracking correct, scoped to streaming branch, test verified red first), no new breakage.
Task 5: complete (commits 3e7cfed..e861749, review clean after 1 fix round).
Task 6: dispatched (implementer agent a9c7ff893053ffd81, sonnet). Deltas R4/R8/R9/R5. Gate default+no-zstd. BASE for review = e861749. Awaiting report (7z builds from source first time).
Task 6: implementer agent a9c7ff893053ffd81 was KILLED (stopped by user) mid-work while verifying the no-task6 link failure. HEAD unchanged at e861749; working tree CLEAN; Task 6 WIP preserved in stash@{0} ("WIP on master: e861749"). No commits made. Paused to check with user (user-initiated stop = intervention signal). Do NOT drop stash@{0} — it holds the Task 6 progress.
Task 6: RESUMED — fresh implementer agent a49be9333d7708f7a (opus) to pop stash@{0} and finish (verify COM sigs, apply R4/R8/R9/R5, add tests, build default+no-zstd, confirm imports). BASE for review = e861749. Awaiting report.
Task 6: RESUMED — DONE. Code commit 73e9081 (12 files, 817 lines). Stash consumed. Implementer had ALSO committed the SDD report into the repo (efc9d8b) — dropped that commit (soft reset + unstage); report kept as ignored scratch, HEAD now 73e9081. Recovered WIP was "already correct": modern Z7_IFACE_COM7_IMP/Z7_COM7F_IMF idiom matching installed 7-Zip 24.06 headers (not the brief's STDMETHOD snippets); R4/R8/R9 already applied; R5 comsuppw NOT needed. imports=KERNEL32 only (rcedit.exe doesn't call the 7z codec until Task 10 wires commands, so OLEAUT32 appears later; KERNEL32-only is a valid subset of the allowlist now). SevenZip round-trip/ContentSize/empty/corrupt test cases already existed from Task 4 and now link+pass. Reviewer dispatched. BASE for review = e861749.
Task 6: review — Spec ✅ (reviewer rebuilt no-zstd from clean + reran ctest/dumpbin; all rulings R4/R8/R9/R5 confirmed; COM refcounting clean). ONE Important finding: out_mem_stream.cpp Write(:39)/SetSize(:74) catch only std::bad_alloc; vector::resize also throws std::length_error when size > max_size(). Corrupt archive with outsized kpidSize → SetSize(huge) → length_error escapes COM boundary out of Decompress (violates no-exceptions-across-boundary). Existing SevenZipRejectsCorrupt only bit-flips, doesn't hit this. Enters fix loop. FIX_BASE = 73e9081.
Task 6: fix round 1 dispatched (resume implementer a49be9333d7708f7a, opus).
Task 6: fix round 1 DONE, code commit 3c5db96 (broadened OutMemStream Write/SetSize catch bad_alloc->std::exception, returns E_OUTOFMEMORY -> surfaces errc::corrupt_payload; new guarded tests/test_sevenzip.cpp 'sevenzip' group: SetSize(UINT64_MAX)+oversized Write; TDD: pre-fix group fast-fails 0xC0000409, post-fix 2/2 pass). no-zstd+default 7/7, no-7z/minimal 6/6, /WX clean. Implementer AGAIN committed the report (a75a893) -> dropped it (soft reset+unstage). Scoped re-review dispatched. FIX_BASE=73e9081 HEAD=3c5db96.
Task 6: fix round 1 re-review — ADDRESSED (both Write/SetSize catch std::exception -> E_OUTOFMEMORY; covering test drives SetSize(UINT64_MAX)/oversized Write asserting FAILED HRESULT + no throw across COM; registration guarded), no new breakage.
Task 6: complete (commits e861749..3c5db96, review clean after 1 fix round).

=== PAUSED after Task 6 at user request ===
Remaining: Task 7 (engine + Win32 impl), Task 8 (operations), Task 9 (arg parser), Task 10 (commands/main/smoke), Task 11 (CI/README). Briefs 7 already staged. To resume: continue subagent-driven from Task 7. Rulings still pending for later tasks: R11 (T9), R12 (T10), R13/R15 (accepted deviations), R16/R17 (T11), R7 (T8 hexdump golden), R5 (T10 link). Do NOT run final whole-branch review or delete workspace until Task 11 done.
NOTE for future implementers: two implementers committed the SDD report file into the repo (dropped both times). Tell implementers NOT to `git add` anything under .superpowers/ — it's scratch (ignored via .superpowers/sdd/.gitignore `*`); the report goes to the report FILE only, not a commit.

=== CROSS-MACHINE HANDOFF (committed to git 2026-09-03) ===
This SDD workspace is now committed so the plan can be resumed on another machine with no access to this one. MACHINE-SPECIFIC facts above must be re-derived on the new machine:
- VS dev-shell path (this machine: VS 18 BuildTools at "C:\Program Files (x86)\...\18\BuildTools\...") — find the local Launch-VsDevShell.ps1; needs C++23 MSVC.
- Absolute S:\ paths (repo on a network share; reference projects S:\llm\dfir-orc-forge and S:\llm\orc\...\OrcCapsule) will NOT exist. The 7zip overlay port is already vendored under external/vcpkg_overlay_ports/ (Task 6 done), so those reference projects are no longer needed for Tasks 7-11. Ignore any brief text pointing at them.
- external/vcpkg is a submodule pinned to cd61e1e2... — run `git submodule update --init` after clone. First configure of a codec preset rebuilds 7-Zip+zstd from source (minutes).
- Git identity not configured: commit with `git -c user.name=fabienfl -c user.email=fabien.fl-orc@ssi.gouv.fr commit ...`.
RESUME: re-invoke superpowers:subagent-driven-development on plan docs/superpowers/plans/2026-09-03-rcedit-v2.md; this ledger's "Task N: complete" lines mark 1-6 done; start at Task 7 (brief already staged as task-7-brief.md). Regenerate review packages with the skill's review-package script as needed. Keep the accepted-deviation and pending rulings above.

=== RESUMED ON NEW MACHINE (2026-09-03) ===
Machine-specific facts re-derived on this machine (H2ODEV network share):
- VS dev shell: `& "C:\Program Files (x86)\Microsoft Visual Studio\18\BuildTools\Common7\Tools\Launch-VsDevShell.ps1" -Arch amd64` (same path as origin machine; vswhere warning benign). Verified: MSVC 19.51.36256 (C++23), CMake 4.3.1-msvc1, Ninja 1.13.2.
- Submodule external/vcpkg initialized at pinned cd61e1e2 (2026.06.24). Needed safe.directory exception for the submodule UNC path (added to global git config: '%(prefix)///H2ODEV/share/llm/rcedit/external/vcpkg'). First codec-preset configure will rebuild 7z+zstd from source.
- Reference projects DO exist here (contra handoff note): S:\llm\orc\dfir-orc\src\OrcCapsule and S:\llm\dfir-orc-forge present.
- No build/ dir yet — first configure bootstraps vcpkg + builds fmt (minimal preset).
Task 7: interfaces spot-checked present & matching brief code (ResourceKey/ResourceId/ToLpcwstr/FromLpcwstr in resource_id.h; errc read_only/invalid_language/empty_payload/resource_not_found + Win32Error/LastWin32Error in error.h; ModuleHandle=std::unique_ptr w/ reset/get/bool in guard.h; FormatError in format.h; Log in log.h). No gaps to flag.
Task 7: BASE for review = b1be789 (HEAD after handoff commit; Task 6 complete at 3c5db96, b1be789 only tracked SDD workspace). Dispatched implementer (general-purpose, sonnet) with brief task-7-brief.md + report task-7-report.md.
Task 7: implementer agent a332fe80b138e266f (sonnet). Awaiting report (first configure bootstraps vcpkg+fmt, minutes).

Task 7: implementer BLOCKED (verified plan defect). Brief's verbatim Read() relies on FindResourceExW raising ERROR_RESOURCE_LANG_NOT_FOUND for a wrong explicit language, but FindResourceExW has documented language fallback: a single-language resource is returned for ANY requested language, so LANG_NOT_FOUND never fires and the wrongLang test (Read(FIXTURE,lang=1033)==resource_not_found) fails. Implementer confirmed via standalone Win32 probes (lang 1033/2052/1036/0xFFFF all succeed against the single-lang fixture). 12/13 engine cases pass; only the wrongLang sub-case fails.
- Ruling R19 (T7 exact-language Read) — Spec line 285 binds: "The engine itself never resolves; it requires key.lang to be set." The neutral/only/ambiguous resolution lives in the OPERATION layer (Get/Hexdump/Remove enumerate, then call the engine with a concrete lang); the engine is a precise exact-language primitive. So the TEST is correct (wrongLang -> resource_not_found) and stays; the brief's Read() IMPLEMENTATION is the defect. Fix: before FindResourceExW, verify the exact requested language is present via EnumResourceLanguagesW (early-stop callback); type/name/lang absent -> errc::resource_not_found. Rejected Option 2 (accept OS fallback, drop the test): it would make `get --lang X` silently return another language's bytes, contradicting the precise-primitive design and undermining the operation layer's only-language/ambiguous logic. Cost if wrong: engine returns fallback bytes for an explicit wrong lang; caught by the wrongLang test which stays red.
- Ruling R20 (T7 fixture manifest) — ACCEPT implementer's `target_link_options(rcedit_fixture PRIVATE /MANIFEST:NO)`: MSVC auto-embeds a default RT_MANIFEST (#24/#1/1033) into the fixture exe, breaking the brief's own "exactly one resource" baseline (EnumerateBaseline asserts size==1). Scoped to the test fixture only; does not touch rcedit's own linker flags or the import-allowlist exe. Mechanical build correction to realize the brief's stated contract, not a design choice. Cost: none.
Task 7: resuming implementer a332fe80b138e266f (sonnet) with R19 exact-language fix (exact code supplied) + R20 accepted. Not a review fix round (no commit yet); BLOCKED-route rule+re-dispatch. BASE for review stays b1be789.
Task 7: implementer DONE, commit ac6e43c (9 files, 803 insertions). engine 13/13 + full minimal-MinSizeRel suite 7/7, /W4 /WX clean, output pristine. R19 applied (RequireExactLanguage via EnumResourceLanguagesW exact-match, wired into Read before FindResourceExW); R20 kept. Commit clean of .superpowers/ paths (verified). Reviewer dispatched (agent af3e07ff5538a488e, sonnet). BASE for review = b1be789.
Task 7: review clean — Spec ✅, Task quality Approved, no Critical/Important. Reviewer traced R19 exact-language branches (early-stop/found-authoritative, absent-language, absent-type/name, lang0 baseline/just-written) all correct; R20 scoped to fixture; confirmed clang-format Step 6 genuinely run (style-diffed vs unmodified files); LastWin32Error/Win32Error use system_category (no COM). Four minors (deferred):
Task 7: minor (deferred): EnumContext.module is a dead field (callbacks use the OS-supplied module param) — drop for clarity.
Task 7: minor (deferred): OnName/OnType FromLpcwstr + OnLanguage push_back can throw bad_alloc inside an OS BOOL CALLBACK with no try/catch up through Enumerate — real but SYSTEMIC (codec.cpp etc. share the no-alloc-guard convention); follow-up across codebase, not a T7-isolated fix. Flag to final review.
Task 7: minor (deferred): RequireExactLanguage fallback uses IsNotFound (excludes 1812 DATA_NOT_FOUND) — effectively unreachable (EnumResourceLanguagesW doesn't return 1812).
Task 7: minor (deferred): tests/CMakeLists.txt inline comment conflates RT_MANIFEST type(24) with manifest name id(1); cosmetic.
Task 7: complete (commits b1be789..ac6e43c, review clean).
Task 8: BASE for review = ac6e43c. R7 resolved before dispatch: FormatHexdump impl is the golden (49-col hex field + 1 format space => the | column is identical on every line by construction). Test line 335 literal has 31 spaces after "65" (WRONG, misaligns |); golden = 30 (aligns with full-16 line 342). Instruct implementer to write the line-335 test literal with 30 spaces, impl unchanged. Dispatching implementer (sonnet).
Task 8: implementer agent a89ecb6096ddf21c2 (sonnet). Deltas: R7 (30-space hexdump literal, impl unchanged); FindCodec returns std::expected. Gate minimal+default. BASE for review = ac6e43c. Awaiting report.
Task 8: implementer DONE, commit a9eb1d5 (6 files, 879 insertions). ops 15/15 minimal + 13/13 default; full suites 8/8 minimal, 9/9 default; output pristine. Commit clean of .superpowers/ (verified). R7 30-space literal applied. Reviewer dispatched (agent a95a6752c937f0db9, sonnet). BASE for review = ac6e43c.
Task 8: review — Spec ✅ except ONE Important. Reviewer verified language resolution (all 4 branches + candidates), codec_disabled propagation (Get errors, List leaves contentSize unset), Remove read-before-write ordering, copy-first/never-open-pe invariant, self_update guard, R7 hexdump columns (30 & 2 spaces, pipes aligned), no-exceptions/[[nodiscard]]. Important finding: Set (ops.cpp ~221-241) compresses BEFORE PrepareTarget (target selection + self_update check), reversing the brief's stated order (empty->target->self_update->copy->compress). Effect: Set(disabled codec, target = running exe) returns codec_disabled instead of self_update. No corruption (neither path writes); wrong refusal reason. Untested combo. Enters fix loop. FIX_BASE = a9eb1d5.
- Ruling R21 (T8 Set order) — Fix precedence so self_update wins over codec_disabled, per the brief. But do NOT simply move the whole PrepareTarget (incl. pe->output copy) before compress: that would leave a stray output=copy-of-pe on a codec_disabled failure (a regression vs current behavior, where compress-first avoids any copy on that failure). Correct order: (1) empty->empty_payload; (2) select target; (3) self_update check (no side effect); (4) compress (no side effect; may fail codec_disabled); (5) copy pe->output for the output case; (6) open target ReadWrite, Write, Commit. Satisfies the brief's precedence AND "copy before write" AND "pe never opened for write", with no stray output on any pre-write failure. Deviates from the brief's literal copy-before-compress but violates no stated invariant and is strictly better. Cost if wrong: on codec_disabled with --output a stray copied file could remain (the exact thing this avoids) or precedence stays wrong (caught by the new combined-case test).
Task 8: fix round 1 dispatched — resume implementer a89ecb6096ddf21c2 (sonnet) with R21 ordering + a covering test (disabled-codec + running-exe target -> self_update).
Task 8: fix round 1 DONE, commit 0535bc7 (ops.cpp reorder per R21; new test SetErrorPrecedenceSelfUpdateBeforeCodecDisabled RED-before/GREEN-after; ops 16/16 minimal + 13/13 default, full suites 8/8 & 9/9). Commit clean of .superpowers/. Scoped re-review dispatched (agent a9aa115e98f8e7158, sonnet). FIX_BASE=a9eb1d5 HEAD=0535bc7.
Task 8: fix round 1 re-review — ADDRESSED (Set order is R21's exact sequence: empty->SelectTarget/self_update->compress/codec_disabled->CopyToOutput->Open/Write/Commit; no stray-output on codec_disabled; Remove unaffected — PrepareTarget cleanly split into SelectTarget+CopyToOutput; covering test guarded per-preset, RED-before/GREEN-after verified), no new breakage.
Task 8: complete (commits ac6e43c..0535bc7, review clean after 1 fix round).

Task 9: BASE for review = 0535bc7. R11 examined: brief HALF-honors it — FormatCommandUsage uses shared OptionLabel + content-computed width (good), but FormatUsage's global-options loop DUPLICATES the label ternary inline AND hardcodes {:<18}. kGlobals is a distinct GlobalFlag struct (not OptionSpec), so full dedup needs a lower-level FlagLabel(shortName,longName) shared by OptionLabel and the globals loop. R11 resolution folded into dispatch: (1) extract FlagLabel(wchar_t,wstring_view) -> "-s, --long" or "    --long"; (2) OptionLabel = FlagLabel + value suffix; (3) FormatUsage globals loop uses FlagLabel + content-computed width (max over kGlobals), formatted "  {:<{}}  {}\n" — no inline ternary, no magic 18. Tests assert substrings only, so width change is safe. Consumes Version()/AnsiToUtf16 (verified present). Gate minimal. Dispatching implementer (sonnet).
Task 9: implementer agent a56f74fd91167c92a (sonnet). Delta R11 (FlagLabel extraction + content-computed global width, exact code supplied). Gate minimal. BASE for review = 0535bc7. Awaiting report.
Task 9: implementer DONE, commit d6f5047 (6 files, 601 insertions). args 12/12 + full minimal suite 9/9, pristine. Commit clean of .superpowers/. R11 FlagLabel + content-width applied. Reviewer dispatched (agent a19eab83a1703b4ce, sonnet). BASE for review = 0535bc7.
Task 9: review clean — Spec ✅, Approved, no Critical/Important/Minor. Reviewer traced token loop, --terminator, ordered error precedence + command threading, expected/[[nodiscard]] channel, R11 (single FlagLabel + content width, no magic 18, OptionLabel output unchanged), heterogeneous map lookup. One ⚠️ (ctest not re-run per instructions) — NOT a gap: implementer report carries the 12/12 + 9/9 evidence, re-run disallowed by design. Confirmed by controller.
Task 9: complete (commits 0535bc7..d6f5047, review clean).

Task 10: BASE for review = d6f5047. Final integration (commands + wmain + cli_smoke), gate ALL FOUR presets + default-Debug. Rulings folded into dispatch:
- R12 (a): brief's ParseLimit (line ~367) accumulates value*10+digit with NO overflow guard -> a huge --limit silently wraps. Add a size_t overflow guard: before each mul-add, if value > (SIZE_MAX - digit)/10 return usage error "--limit ... too large". Exact code supplied.
- R12 (b): Run* handlers (RunHexdump etc.) deref *key/*limit/*codec blindly; std::expected::operator* in the error state is UB. Add defensive .has_value() checks before every deref of a ParseKey/ParseLimit/ParseCompress/value result; on the (validate-guaranteed-unreachable) failure return kUsage. Thin safety net, no restructure — example given for one handler, applied to all.
- R5: brief Step 6 already runs all four presets + the imports matrix (minimal/no-7z = KERNEL32 only; no-zstd/default = KERNEL32+OLEAUT32) and says stop+report on any other DLL/symbol outside kernel32/oleaut32/uuid/comsuppw. Remind implementer: do NOT add libraries; report exact DLL/symbol names if the link pulls a new import, and confirm the imports CTest passes on all four.
Dispatching implementer (sonnet).
Task 10: implementer agent a08d88a743c324b17 (sonnet). Deltas R12(a) ParseLimit overflow guard + R12(b) Run* defensive has_value (exact code supplied), R5 import matrix strict. Gate ALL FOUR presets + default-Debug. BASE for review = d6f5047. Awaiting report (no-7z/no-zstd first configure may rebuild a codec).

Task 10: implementer BLOCKED (real R5 landmine, correctly stopped). rcedit.exe now imports USER32.dll (CharUpperW) on the 7z-enabled presets (no-zstd, default) — first build where codec_7z.cpp is actually linked into the exe. Root cause: vendored 7-Zip SDK CPP/Common/MyString.h MyCharUpper() calls CharUpperW in the _WIN32 && _UNICODE branch (line 178), reached via the CreateObject/archive-open path. imports = KERNEL32+USER32+OLEAUT32 (expected KERNEL32+OLEAUT32). minimal/no-7z clean. No link failure; comsuppw not needed. Impl otherwise complete + R12(a)/(b) applied (uncommitted).
- Ruling R22 (T10 eliminate USER32) — ELIMINATE the USER32 import at the source rather than widen the allowlist. Rationale: the allowlist ("KERNEL32 always; OLEAUT32 only w/ 7z; nothing else, ever") is a user-stated hard constraint + a "do not re-open" decision; widening it to USER32 changes the tool's headline security boundary (security-sensitive) — not something to do unilaterally. Elimination preserves the constraint and is clean here: the 7zip overlay port ALREADY patches 7-Zip via portfile.cmake PATCHES (3 patches present), so adding one is idiomatic. The fix is a one-line, provably-safe patch: MyString.h MyCharUpper()'s _WIN32&&_UNICODE branch (line 178) currently `return ...CharUpperW(...)`; change to `return (wchar_t)towupper((wint_t)c);` — IDENTICAL to the file's own non-_WIN32 branch (line 183) and <wctype.h> already included (line 9). ASCII (c<=0x7F) is fully handled inline BEFORE this branch, so CharUpperW only ran for c>0x7F, which rcedit (fixed ASCII entry name "payload", reads index 0 per R9) never reaches at runtime -> behavior identical for rcedit; towupper is CRT (static, no USER32). Cost if wrong: USER32 persists (another caller) or a 7z name-compare differs for non-ASCII (rcedit never has non-ASCII names) — caught by the imports CTest + sevenzip/cli_smoke_7z. FLAG PROMINENTLY to the user at finish: they may prefer to instead allowlist USER32 (a side-load-safe KnownDLL) and drop the 7-Zip patch.
Task 10: BLOCKED-route rule+re-dispatch — resume implementer a08d88a743c324b17 (sonnet) with R22 (add overlay-port patch, force 7zip rebuild, verify imports matrix + all 7z tests, then commit Task 10 code+patch together). Do NOT widen the allowlist.

Task 10: R22 patch WORKED — all four MinSizeRel (shipped) presets clean, imports matrix exact (KERNEL32 / KERNEL32+OLEAUT32, no USER32). New finding: default-Debug imports fails on a DIFFERENT USER32 symbol, CharPrevExA, from HasTailSlash (ItemNameUtils.cpp:116) — Debug-ONLY (absent from every MinSizeRel build). Implementer correctly stopped again.
- Ruling R23 (T10 imports = release-artifact property; exempt Debug) — Investigation: ~10 USER32 Char* call sites exist across the vendored 7-Zip SDK (CharLowerW x4, CharUpperW x4, CharPrevExA x2, CharNextA, CharPrevA, CharUpperA x2). Release (MinSizeRel/RelWithDebInfo) inlines + /OPT:REF strips every unreachable one; only CharUpperW was reachable in release and R22 patched it. In Debug (un-optimized) they linger. HasTailSlash's CharPrevExA is guarded by `if (codePage != CP_UTF8)` and rcedit is ALWAYS CP_UTF8 -> never called at runtime, only linked un-inlined in Debug. RULING: the import allowlist protects the DISTRIBUTED (release) binary, which is clean; Debug builds are dev-only, never shipped. Do NOT patch CharPrevExA or the other ~8 sites (fragile whack-a-mole, zero shipped-artifact benefit). Instead exempt the Debug config from the imports CTest: pass $<CONFIG> into check_imports.cmake; skip (STATUS + return()) when CONFIG==Debug. Release configs stay strictly enforced. Keep the R22 CharUpperW patch (it fixed a real release import). Cost if wrong: a Debug build could import a benign guarded USER32 symbol (never shipped, never reached) — release enforcement unaffected. FLAG to user at finish alongside R22: alternative was to allowlist USER32 (KnownDLL) or patch all ~10 sites and keep Debug strict.
Task 10: resume implementer a08d88a743c324b17 (sonnet) with R23 (exempt Debug from imports; do NOT patch further), then commit Task 10 (code + R12 + R22 port patch + R23 imports-infra) as one commit.

Task 10: implementer DONE, commit d7c152b (12 files) — commands+wmain+cli_smoke + R12(a)/(b) + R22 port patch + R23 Debug-skip. All four MinSizeRel green, strict imports (no USER32); default-Debug 13/13, imports skipped.
=== USER DECISION (overrides R22/R23) ===
User: "user32.dll is fine as other defined in knowndlls." USER32 is a KnownDLL (loaded from protected \KnownDlls, not side-loadable), security-equivalent to the already-allowed OLEAUT32. This reaffirms/relaxes the import-allowlist decision and is the user's call.
- Ruling R24 (T10 allowlist USER32; SUPERSEDES R22 + R23) — Per user decision: allow USER32.dll in the import allowlist when RCEDIT_ENABLE_7Z (same condition as OLEAUT32), and REVERT the workarounds that avoided it: (1) revert the R22 7-Zip overlay-port patch (delete mycharupper-no-user32.patch, remove it from portfile.cmake PATCHES, revert the vcpkg.json port-version bump); (2) revert the R23 Debug exemption in check_imports.cmake (remove the CONFIG==Debug early-return) and the -DCONFIG=$<CONFIG> add in tests/CMakeLists.txt. Net: no vendored patch, no config-conditional test logic. New import matrix: minimal/no-7z = KERNEL32 only; no-zstd/default = KERNEL32+OLEAUT32+USER32 (all configs incl. Debug). Keep R12(a)/(b) (unrelated, valid). In-flight Task 10 reviewer (acd00570e6369baf6) was reviewing the patch+exemption approach — STOPPED (redundant; will re-review the final state). Cost: none — USER32 is a KnownDLL; the allowlist still blocks any non-KnownDLL side-loadable import.
Task 10: resume implementer a08d88a743c324b17 (sonnet) with R24 (revert patch+exemption, allowlist USER32, revalidate all presets + Debug, commit). Then fresh review of final state. BASE for review stays d6f5047.
Task 10: implementer DONE (final state), follow-up commit b301c34 on d7c152b — R24 applied (USER32 allowlisted 7z-conditional; R22 patch + R23 exemption reverted; net tree has no mycharupper patch, no Debug skip). All four presets + default-Debug green; imports minimal/no-7z=KERNEL32, no-zstd/default=KERNEL32+OLEAUT32+USER32. R12(a)/(b) kept. Fresh reviewer on final state d6f5047..b301c34 dispatched (agent a4e91f769e8be7dd6, sonnet). BASE for review = d6f5047.
Task 10: review clean (final state) — Spec ✅, Approved, no Critical/Important. Reviewer independently verified via live dumpbin on all four presets + default-Debug: minimal/no-7z=KERNEL32; no-zstd/default(MinSizeRel+Debug)=KERNEL32+USER32+OLEAUT32 — exactly the R24 allowlist, no leftover patch/Debug-skip (port + check_imports.cmake net-zero vs d6f5047). Command table, validate/run split, R12(a)/(b) in all 5 handlers, wmain exit codes, cli_smoke --output non-mutation all confirmed against real APIs.
Task 10: minor (deferred, pre-existing, FLAG to final review): src/cli/CMakeLists.txt:18 comment "explicit allowlist / no default libraries" overstates reality — MSVC's implicit CMAKE_CXX_STANDARD_LIBRARIES still puts gdi32/shell32/ole32/comdlg32/advapi32/winspool on the link line regardless of /NODEFAULTLIB + the explicit list. Functionally OK (none are IMPORTED; imports CTest checks the real PE import table, which is clean), but the comment/mechanism is imprecise re: the anti-side-loading intent. Worth a Task-1-scope comment fix or note.
Task 10: complete (commits d6f5047..b301c34, review clean; incl. user decision R24).

Task 11: BASE for review = b301c34. CI workflow + README. Deltas folded into dispatch:
- R3/PDB: brief Step-1 says add /Zi + /DEBUG /OPT:REF /OPT:ICF to TOP-LEVEL CMakeLists. ALREADY DONE by Task 1: /Zi at CMakeLists.txt:64, /DEBUG /OPT:REF /OPT:ICF at src/cli/CMakeLists.txt:19 (all-config), and build/default/MinSizeRel/rcedit.pdb EXISTS. So SKIP the CMakeLists edit (would duplicate/conflict) — just verify the pdb exists. Matches R3's prediction ("T11 need not edit CMakeLists").
- R16: brief workflow uploads artifact `if: matrix.preset == 'default'` only (NOT tag-gated). Fix: `if: matrix.preset == 'default' && startsWith(github.ref, 'refs/tags/')` — satisfies spec "on tags" AND makes github.ref_name safe (tag matches v[0-9]+..., filename-safe; no unsanitized branch ref in artifact name).
- R17: actual presets verified — default has -Debug/-RelWithDebInfo/-MinSizeRel; no-7z/no-zstd/minimal have -MinSizeRel ONLY. Brief README line "Each has -Debug, -RelWithDebInfo (default only) and -MinSizeRel" is inaccurate; rewrite to state default-only for Debug/RelWithDebInfo, MinSizeRel-only for the other three.
- R24 consequence (README accuracy): brief README says exe "imports nothing but KERNEL32.dll (plus OLEAUT32.dll ... when 7z)". Must ALSO list USER32.dll (both KnownDLLs) for the 7z build, matching the final allowlist.
- CI vcvars path (VS2022 Enterprise / windows-2025) is for GitHub hosted runners, NOT this machine's VS18 — do NOT change to local path; workflow only verifiable by pushing (note in report).
Dispatching implementer (sonnet).
Task 10: complete recorded above. 
Task 11: implementer agent ae0aa4d41b9352c43 (sonnet). Deltas: R3 skip CMakeLists edit (pdb already exists), R16 tag-gate artifact, R17 preset accuracy, R24 USER32 in README. Gate: verify pdb + default-MinSizeRel ctest (no code change). BASE for review = b301c34. Awaiting report.
Task 11: implementer DONE, commit 6b10d24 (2 files: build.yml 70, README.md 78). pdb exists=True; default-MinSizeRel ctest 13/13. No CMakeLists touched (R3 respected). Commit clean of .superpowers/. Reviewer dispatched (agent a4be4b307237a8503, sonnet). BASE for review = b301c34.
Task 11: review clean — Spec ✅, Approved, no Critical/Important. R16 tag gate, R17 preset accuracy, R24 USER32 in README, R3 no-CMakeLists-touch all verified exact; only the two intended files changed; brief templates transcribed verbatim otherwise. Three minors (deferred, CI hygiene) + push-only ⚠️ items:
Task 11: minor (deferred): build.yml has no `permissions:` block (default GITHUB_TOKEN broader than needed; `permissions: contents: read` would harden).
Task 11: minor (deferred): build.yml never exercises default's -RelWithDebInfo (only -MinSizeRel + -Debug); coverage gap, matches brief design.
Task 11: minor (deferred): build.yml has no `timeout-minutes` (hung step could hit 6h default cap).
Task 11: UNVERIFIABLE LOCALLY (⚠️, needs a GitHub push — NOT done, requires user consent for a shared-branch side effect): windows-2025 Ninja/VS2022-Enterprise paths, preset-name resolution on the runner, vcpkg cache key/path vs actual binaryDir.
Task 11: complete (commits b301c34..6b10d24, review clean).

=== ALL 11 TASKS COMPLETE (Tasks 1-6 on origin machine; 7-11 this session) ===
Now: final whole-branch review (most capable model) over the feature range, pointed at deferred minors + rulings. Do NOT delete workspace or push until user decides.
Final whole-branch review: dispatched (agent a23896d63e683d848, OPUS) over OUR code f5afff9..6b10d24 (filtered diff, external/ excluded as vendored). Pointed at deferred-minors triage + accepted deviations (R9/R13/R14/R15/R19/R21/R24) + cross-cutting focus (exception-safety across OS/COM boundaries, security/allowlist model, integration coherence, test integrity). MERGE_BASE = f5afff9.

=== FINAL WHOLE-BRANCH REVIEW (opus) — Ready to merge: YES ===
No Critical. One Important (Issue 1: bad_alloc/exceptions inside OS enumeration callbacks + 7z COM methods with no try/catch to the boundary — violates "no exceptions across a library boundary"; reviewer triaged POST-MERGE: reachable only under OOM, the attacker-driven length_error case already guarded+tested in OutMemStream). Minors 2-10 all triaged POST-MERGE. Verdict: layers integrate coherently, codec_disabled never silent-falls-back, COM refcounting correct, imports allowlist genuinely gates the PE import table across all presets in CI.
Decision: run ONE final fix wave for the worthwhile+low-risk items; defer the rest (ledger).
FIX WAVE (one dispatch): 
 - Issue 1 (Important, binding invariant): add try/catch guards — enumeration callbacks (engine_win32 OnLanguage/OnName/OnType) `catch(...) return FALSE`; COM methods (extract_callback GetStream, update_callback GetStream/GetProperty) `catch(...) return E_OUTOFMEMORY`.
 - Issue 2 (correctness wart): Set/Remove delete the *output file on the failure path after CopyToOutput ran, so a failed `--output` op leaves no artifact.
 - Issue 5 (trivial): RequireExactLanguage treat 1812 ERROR_RESOURCE_DATA_NOT_FOUND as resource_not_found too.
 - Issue 8 (CI hardening): build.yml add `permissions: contents: read` + `timeout-minutes`.
 - Issue 4/6/7 (cost-free): drop dead EnumContext.module field; fix RT_MANIFEST type/name comment; correct the src/cli/CMakeLists.txt allowlist comment to note MSVC implicit standard libs (import table is what's gated).
DEFERRED (post-merge, user's call): Issue 3 (List fails wholesale on one unreadable resource — best-effort skip is a semantic/design change); Issue 9 (CMAKE_CONFIGURATION_TYPES CACHE FORCE — low-impact build config); Issue 10 (ambiguous-language CLI candidate-listing test gap — needs a multi-language fixture).
Final fix wave dispatched (fresh implementer, sonnet). FIX_BASE = 6b10d24.
Final fix wave: dispatched (fresh implementer a71f2bd6c77491325, sonnet). Fixes 1(callback guards)/2(no stray output)/3(1812)/4(CI hardening)/5(cleanups). Deferred: Issue 3/9/10. FIX_BASE = 6b10d24. Awaiting report; scoped re-review after.
Final fix wave DONE, commit 648d315 (8 files, +200/-60). All presets + Debug green, /WX clean, imports matrix unchanged. Note: Fix 2 introduced a Discard() behavior change (m_module.reset() to release the exclusive read mapping so a failed Remove --output can delete the copy). Commit clean of .superpowers/. Scoped re-review dispatched (agent a50f465f6476de506, sonnet) — verifies Fixes 1-5 + the Discard change safety. FIX_BASE=6b10d24 HEAD=648d315.
Final fix wave: re-review clean — all 5 findings ADDRESSED; Discard() m_module.reset() change verified SAFE (no-op on every pre-existing call site: Open re-resets harmlessly, BeginUpdate already nulls m_module before m_update, destructor only discards when m_update set; new RemoveFailureAfterCopyRemovesOutput test proves the lock fix). No new breakage. Two non-blocking out-of-scope notes: no Write/Commit-stage Set failure test (reasoning gap, not functional — same RemoveStrayOutput path is Open-stage tested); RemoveStrayOutput deletes a pre-existing --output file on failure (pre-existing: CopyToOutput already overwrites on success).
Final fix wave: complete (commit 648d315, re-review clean).

=== PROJECT COMPLETE ===
All 11 tasks complete + individually reviewed. Final whole-branch review (opus): Ready to merge = YES. Final fix wave (648d315) applied + re-reviewed clean. HEAD = 648d315 on master (local; origin/master behind at b1be789 — NOT pushed). SDD workspace NOT deleted (git-tracked for cross-machine handoff; user decides integration). Awaiting user decision on: push to origin/master, and whether to keep/remove the tracked SDD workspace. Do NOT push or delete without user consent.
